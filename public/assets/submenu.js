let image=document.getElementById("nav-user-icon");
let subMenu=document.getElementById("subMenu");

// image.addEventListener("onclick",toggleMenu());
function toggleMenu() {
    subMenu.classList.toggle("open-menu");
}